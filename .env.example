# Database (defaults to local Docker Postgres if not set)
DATABASE_URL=postgresql+psycopg2://signlang:signlang_dev_pw@postgres:5432/signlang_db

# Email (Forgot Password flow) — free Gmail SMTP with App Password
EMAIL_HOST=smtp.gmail.com
EMAIL_APP_PASSWORD=

# Certificate PDF generation
PDF_OUTPUT_DIR=/app/certificates
