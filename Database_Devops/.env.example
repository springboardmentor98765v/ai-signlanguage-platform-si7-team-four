# Local Docker Postgres (fallback / offline dev)
# DATABASE_URL=postgresql+psycopg2://signlang:signlang_dev_pw@localhost:5432/signlang_db

# Shared Neon cloud database (Milestone 2 — ask Intern 5 for the real value)
DATABASE_URL=postgresql+psycopg2://<user>:<password>@<your-neon-host>/<dbname>?sslmode=require
